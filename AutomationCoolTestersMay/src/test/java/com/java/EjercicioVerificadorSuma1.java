/*package com.java;

public class EjercicioVerificadorSuma {
    public static void main(String[] args) {
        /*
         * int x = 1; int y = 1; int sum = 1; boolean valorSuma = suma(x, y, sum);
         * System.out.println("La suma de: " + x + "+" + y + " es " + sum + " " +
         * valorSuma);
         * 
         * sum = 2; valorSuma = suma(x, y, sum); System.out.println("La suma de: " + x +
         * "+" + y + " es " + sum + " " + valorSuma);
        

        hasEqualSum(1, 1, 1);
        hasEqualSum(1, 1, 2);
    }

    public static void hasEqualSum(int x, int y, int z) {
        boolean resultado = suma(x, y, z);
        {
            System.out.println("La suma de: " + x + "+" + y + " es " + "= " + z + " " + "por lo tanto es " + resultado);
        }
    }

    public static boolean suma(int x, int y, int z) {
        if ((x + y) == z) {
            return true;
        } else {
            return false;
        }
    }
}
*/
